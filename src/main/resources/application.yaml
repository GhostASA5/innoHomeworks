spring:
  application:
    name: bank

  liquibase:
    enabled: true
    change-log: classpath:db.changelog/db.changelog-master.xml

  datasource:
    url: jdbc:postgresql://localhost:5437/bank
    username: postgres
    password: postgres


server:
  port: 8090

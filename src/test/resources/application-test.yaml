spring:
  application:
    name: notes

  datasource:
    url: jdbc:postgresql://localhost:5435/notes_db
    username: postgres
    password: postgres
  jpa:
    generate-ddl: true
    open-in-view: true

management:
  endpoints:
    access:
      default: none

